require("../CatalogueInterface");
require("consoloid-framework/Consoloid/Test/UnitTest");
describeUnitTest('Consoloid.OS.File.CatalogueInterface', function() {
  describe("#getFileInfosFromCatalogueAsync(callback, name)", function() {
    it("should return with the list of basic information of the files in a catalogue")
  });
  describe("#getFileChunkAsync(callback, catalogueName, fileId, offset, chunkSize)", function() {
    it("should return with the chunk in a Uint8Array")
  });
});